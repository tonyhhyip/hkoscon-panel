const attendee = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

const attendees = (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default attendees;